# C422 — `web4-lct` 10th delta audit

**Target**: `web4-standard/protocols/web4-lct.md` — blob `5f68a5c7`, **278 lines** (`wc -l`; the last
content line is **279** — the file has no trailing newline, see §F.1), mover `27b85624` 2026-02-17.
**Byte-frozen 184 days; 10th consecutive frozen pass.**

**Window**: `7be587dd..HEAD` = **26** commits. **ZERO mutation.** 1 new file (this document).

**Lineage (11 prior members, inclusive rule)**: `C60-B13` → **C74** (#363, origin audit, 28 findings
B1–B28 + 5 refuted) → **C75** (#364, `protocols/` cluster triage, structural defects 3.1/3.2) → C114
(1st delta) → C146 (2nd) → C186 (3rd) → C224 (4th) → C262 (5th) → C302 (6th) → C342 (7th) → **C382**
(8th, #712) → **C422** (this, **9th delta / 10th pass over the frozen blob**).

**Headline**: *this pass drafted two findings and policy review killed both — the 23rd consecutive
falsified headline in this lineage. What survives is the one member of a four-item block that eight
passes never examined: the target's §9 requests a registry the corpus names three different ways and
holds no file for.*

---

## Conventions

Every gate cell carries the command that produced it (v45). Counts carry their denominator (v40).
Absence claims carry their matcher (v44) **and the matcher's filter** (v75). `HEAD` = `4be3b109`.

---

## §A — Freeze verification (collapsed to blob identity, C344 policy precedent)

| Artifact | C382 baseline | at `HEAD` | moved? |
|---|---|---|---|
| target `protocols/web4-lct.md` | blob `5f68a5c7` | `5f68a5c7` | **no** |
| canonical `core-spec/LCT-linked-context-token.md` | mover `d89595e8` | `d89595e8` | **no** |
| `web4-standard/README.md` | blob `a8eb4120` | **`10aa3e31`** | **YES — in-window** (`2462881f`, #727) |
| `web4-core/src/lct.rs` | mover `2ec6ae09` | `2ec6ae09` | no |
| `web4-core/src/attestation.rs` | mover `0e997079` | `0e997079` | no |
| `web4-core/src/ratchet.rs` | mover `7b048a78` | `7b048a78` | no |
| `web4-standard/schemas/lct.schema.json` | mover `9bcfe598` | `9bcfe598` | no |

`git rev-parse HEAD:<path>` / `git log -1 --format=%h -- <path>` for every row.

**C74 B1–B28 + C114-N1 + C75 3.1/3.2 hold by construction** (target and canonical both byte-identical to
the C382 snapshot). Not re-tabulated — C302/C342/C382 each published the 28-row table; re-printing it is
the collapse C344 forbade. Two rows are **strengthened without being re-charged** in §B.2 and §B.3.

**C382 §I.1 — the corrected event trigger DID NOT FIRE.** This is the first pass at which the corrected
form was available (C382 replaced a directory operand with three file operands after it read
false-positive 2 of 2). All three baselines unmoved ⇒ **silent, correctly.** Stated coverage gap,
carried forward unchanged: `web4-core/src/lib.rs`, `web4-core/python/`.

**C382 §I.2 — the one published matcher, re-run, not re-derived:**

```
git grep -lE '"(lct_id|entity_type|birth_certificate|binding_proof)"' -- web4-standard/<tree>
  rfcs 0 · testing 1 · docs 0 · proposals 0 · submission 1 · profiles 0
```

**Identical to C382's `0/1/0/0/1/0`.** No tree re-derived. **§B.5 stays closed.**

---

## §B — Machine checks (run before the scope proposal, per the standing opening sequence)

### B.1 — C382 §I.3: the routing IS failed, and this time it is measured against the ledger

C382 pre-registered: *"N1 + N2 are ONE routing to the outward-artifact owner. If still open at C422,
that is two passes; report it as a routing failure — probe the **receiver's ledger**, not its spec
files."*

The scope proposal first ran `git log 7be587dd..HEAD -- web4-standard/submission/ …` (0 commits) — which
is **the spec-file instrument the carry explicitly forbids**. Policy review caught it. Re-run correctly:

| Probe | Command | Result |
|---|---|---|
| ledger tree exists? | `ls ledgers/` | 7 entries (`act-chain`, `spec`, `reference`, `ARCHIVIST.md`, `PUBLISHER.md`, `README.md`, `TERMINOLOGY.md`) |
| ledger records the routing? | `grep -rli "draft-palatov\|draft-web4-core\|C382" ledgers/` | **0 files** |
| forum acknowledgement? | `grep -rli "draft-palatov\|SUBMISSION_PROCESS" forum/` | **0 files** |
| any branch moved the artifacts? | `git log --all --since=2026-08-13 -- web4-standard/submission/ web4-standard/SUBMISSION_PROCESS.md` | **0 commits** |

**Verdict: ROUTING FAILURE, confirmed on the receiver's side.** Stated honestly: this corpus has **no
per-finding routing ledger** — `ledgers/` is a spec/reference tree, not a receipt log. So "probe the
receiver's ledger" resolves here to *there is no surface on which delivery could have been recorded*.
That is a **stronger** result than the `git log`, and it is a standing instrument gap worth more than
this row: **C382-N1/N2 are re-routed, and the absence of any receipt surface is recorded as INFO-2.**

### B.2 — NEW: the target's §1 canonical object executed against the enforced schema — first time in ten passes

jsonschema 4.26.0, `Draft202012Validator.iter_errors` read directly (not the non-raising SDK
`validate()`), instance = §1 `:10-49` with `//` comments stripped by a string-aware scanner and the
**one missing `}` supplied** (without it there is no instance to validate — that is B1).

**15 errors. ZERO net-new.** The mapping is the point:

| Schema error | Already charged as |
|---|---|
| `/binding/entity_type` 12 values vs schema's 15 | **C74-B7** (HIGH) |
| `/birth_certificate` — `issuing_society` is required, absent | **C74-B9** (HIGH) |
| `/birth_certificate/context` 3 values vs 5 | **C74-B2** |
| `/lineage/0/reason` `"rotate"` ∉ enum | **C74-B3** |
| `/revocation/reason` 2 values vs 3 | **C74-B4** |
| `/lct_id` missing the `mb32:` colon segment | **C74-B13** |
| `/subject` `did:web4:key:z6Mk…` vs the method's syntax | **C74-B15** |
| `/mrh/witnessing/0/role` 3 values vs 8 | **C74-B6 / B17** axis |
| 6 further errors | placeholder-notation artifacts (`"lct:web4:..."`, `"a\|b\|c"`) — **not findings** |

**This is a positive confirmation, and it is indexed here precisely because a positive confirmation
otherwise gets no row, no disposition and no carry (v74).** The machine, run for the first time in ten
passes, independently reproduces C74's hand-derived clusters 1–2 and adds nothing. Any successor
tempted to run it should read this cell instead.

### B.3 — C74-B1 finally carries a denominator (v46), 8 passes after it was filed

B1 (**HIGH**, HELD 8×) says the §1 "canonical LCT object" does not parse — 13 `{` / 12 `}`. No pass ever
asked whether that is a **corpus idiom**. Measured, string-aware (brace counting after blanking string
literals), over `git ls-files web4-standard` filtered to `.md`:

```
total ```json blocks:            252
brace/bracket-unbalanced:          1   → web4-standard/protocols/web4-lct.md:9-50  (13 { / 12 })
```

**1 of 252. B1 is a singleton, not an idiom.** The corpus balances its fenced JSON everywhere else,
including the canonical spec's own §2.3 object (`:90-216`, parses clean). B1 is **not re-charged** — it
is a HELD row that now has the denominator that decides it (v46), and the denominator makes it stronger.

---

## §C — C422-N1 (LOW-MED, net-new → C70 registries-canonicity bundle): the target requests a registry under a name nothing else uses, for a registry that does not exist

### C.1 — Enumerate the whole block (standing rule)

C74-B19 charged §9. It anchored `L277–279` and named **two** items. §9 has **four**:

| # | `web4-lct.md` §9 item | prior disposition |
|---|---|---|
| 1 | `:276` — LCT URI scheme: `lct:web4:` | **C74 REFUTED** ("the registration request does not conflict") |
| 2 | `:277` — Entity types registry | **C74-B19** (MED, HELD 8×) |
| 3 | **`:278` — Witness type registry** | **never examined, 10 passes** |
| 4 | `:279` — Revocation reason codes | **C74-B19** |

B19's anchor is **exact** — verified, not assumed. The scope proposal initially read §9 as three items,
because `sed -n '272,278p'` truncated at a range end taken from `wc -l` = 278 while the last content
line is 279 (no trailing newline). Recorded as an own-error, §F.1.

### C.2 — The requested registry has three names and no file

| Locus | Name it requests | Value space at the locus |
|---|---|---|
| **`protocols/web4-lct.md:278`** (target) | **"Witness type registry"** | none given |
| `protocols/web4-witness.md:272-274` | "Web4 Witness Types" · "Web4 Witness Claims" · "Web4 Witness Methods" | none given |
| `WEB4_WITNESSING_SPECIFICATION.md:101` ≡ `protocols/web4-witnessing.md:101` | "Web4 Witness Role Registry" | `time` · `audit-minimal` · `oracle` |

The last two paths are **byte-identical** (`git hash-object` = `7984a8e5` for both) ⇒ **one authoring
decision, not two** (v72). So: **three names, four paths, three authoring decisions.**

```
git ls-files web4-standard/registries/   → 5  (README, cipher-suites, error-codes, extensions, initial-registries)
grep -ci witness web4-standard/registries/README.md → 0
```

**No `registries/` file, and no row in the registry cluster's own table, under any of the three names.**

And the target does not agree with itself about what would be registered: §2.4 `:97` types the field as
`role` with 3 values, §6.1 `:219-229` publishes a **7-row "Witness Classes"** table with column header
`Class`, and §6.2 `:236` serialises the key as `"type"`. Three words — `role`, `class`, `type` — in the
document that requests a *"Witness **type** registry"*.

### C.3 — This is the rung below B17, not a re-opening of it (v61)

C74-**B17** (HELD 8×) charges the target's `audit` for not being in the Web4 Witness Role Registry.
C302-**N3** charges the 8th token `peer`. Both argue about **membership of a registry**. Neither asks
whether that registry **exists**, or whether it is the registry the target's own §9 requests. It is not:
B17 measures the target against a registry named at `WEB4_WITNESSING_SPECIFICATION.md:101`; §9 `:278`
asks IANA for a differently-named one. **The 3-vs-7 axis (B6) and the `audit`/`audit-minimal` axis (B17)
are C74's and are NOT re-opened** — per `C302:383`.

`WEB4_WITNESSING_SPECIFICATION.md:17` carries the load-bearing modality: *"Roles are extensible; new
roles **MUST** be registered in the Web4 Witness Role Registry."* A MUST whose registry has no file.
**Cited at the root twin, deliberately** — same bytes as `protocols/web4-witnessing.md:17`, and the root
path is **outside the C75 scope bar**.

### C.4 — Novelty (v44 — absence claim, matcher AND filter published)

```
grep -rn "Witness type registry\|Web4 Witness Types\|Web4 Witness Claims\|Web4 Witness Methods" \
     docs/audits/ web4-standard/docs/audits/          → 0     (denominator: 255 audit documents, both trees)
grep -rnoE 'web4-lct\.md[^ ]{0,4}:2[7-9][0-9]' docs/audits/ web4-standard/docs/audits/
                                                      → the only §9 anchor in the record is C74-B19's
```

**Filter disclosed**: both matchers are content greps over `docs/audits/**` and
`web4-standard/docs/audits/**`, unfiltered by filetype. They cannot see an audit that discusses the
registry without naming it — which is why C.3 also walks B17/B6/N3 by hand.

### C.5 — Refutation of my own finding (standing rule)

- **R1 — "§9 is a wish-list; unimplemented registration requests are normal in a draft."** Partly
  sustained, and it is why this is **LOW-MED, not MED**. What is not normal is *three names for one
  registry across three authoring decisions*, none matching the cluster that declares itself the
  standard's IANA Considerations.
- **R2 — "B19 already covers §9."** Refuted: B19's own text names items 2 and 4 and its harm is
  *duplicate canonical homes*. Item 3 has no canonical home to duplicate — the opposite defect.
- **R3 — "this is really a `protocols/` sister finding, barred by C75."** Sustained *as to the sisters*
  — which is why the charge is laid at `web4-lct.md:278`, the target's own line, with the sisters as
  evidence only (`C296:101`).

**Disposition**: joins the **C70 registries-canonicity bundle** beside B19 (precedent `C52-B3d` — new
per-file evidence joins a standing bundle rather than opening a parallel row). **ZERO mutation.**

---

## §D — C422-I1 (INFO): a scope-widening of C75 structural defect 3.1, not a net-new finding

`web4-standard/README.md:164` ends: *"See LCT spec §1.2."* The README's sole link labelled *"Linked
Context Token specification"* is `:64` → `protocols/web4-lct.md`, which has **never** held a §1.2
(`git log --all -S "### 1.2" -- web4-standard/protocols/web4-lct.md` → **0**; the target's subsections
begin at `### 2.1`). §1.2 exists only in `core-spec/LCT-linked-context-token.md`, which this README
names **zero** times.

**Direction tested**: `README:164` and canonical §1.2 arrived in the **same commit** `d89595e8` (#531,
2026-07-16) — five months after the `:64` link froze (`27b85624`, 2026-02-17).

**Why this is I1 and not N2.** C75 3.1's stated harm is verbatim this mechanism
(`web4-standard/docs/audits/C75-protocols-cluster-lifecycle-triage.md:54`): *"A reader navigating the
README is sent to the frozen sister as the LCT spec and is never pointed at the live canonical doc at
all."* This is that harm's **second symptom**, not a second defect. The lineage has a binding precedent
for exactly this call — `C186:19` widened C172-N1 rather than claiming a novel finding. Same standard
applied here.

**Two mitigations, both of which my first matcher hid** (v73 — an over-narrow matcher is a claim about
your instrument):

1. `grep -rn "See LCT spec"` returns **1** site. The correct matcher `LCT spec §1.2` returns **5**
   non-audit sites: `README.md:164`, `core-spec/t3-v3-tensors.md:16`, `CLAUDE.md:192`,
   `web4-core/src/lct.rs:178`, `:224`. My matcher required the literal word *"See"* immediately before;
   t3-v3 writes *"See **the** LCT spec §1.2, *Inspectable Evidence, Not Prescribed Trust*"* — i.e. the
   site that carries the section **title inline**, the one that makes the citation recoverable, was the
   one my matcher could not see.
2. `README:164` sits under `### 9. Inspectable Evidence, Not Prescribed Trust`, and canonical
   `LCT-linked-context-token.md:26` is `### 1.2 Design Principle: Inspectable Evidence, Not Prescribed
   Trust`. **The README heading is the canonical section's own title** — self-disambiguating. With the
   root `README.md:402` canonical link (`C146:45`) this is recoverable, so: **INFO**.

Also indexed, because it is a positive confirmation that carried no row (v74): **`C210:61` already
recorded `t3-v3-tensors.md:16` as "Landed"** and `C210:62` recorded `CLAUDE.md:192`. The §1.2 citation
graph was measured ten months ago by another lineage and filed as consistent.

---

## §E — INFO-2 (instrument, corpus-wide): there is no surface on which a routing can be received

§B.1 established it: `ledgers/` is a spec/reference tree; nothing in the corpus records that a routed
finding reached its owner. Every C-series pass routes findings to "the operator", "the standard editor",
"the outward-artifact owner" — and **v32/v36 ("routing by slot number is not delivery"; "delivery is an
act of the receiver") cannot be discharged by any instrument this corpus provides.** C382's §I.3
pre-registration asked its successor to do something the corpus makes impossible. Recorded, not charged.

---

## §F — This pass's own errors (7)

1. **§9 read as three items** — `sed -n '272,278p'` with a range end taken from `wc -l` = 278, while the
   file's last content line is 279 (no trailing newline). Nearly filed C74-B19's anchor `L277–279` as
   wrong; it is exact. Caught by policy review. *A file without a trailing newline has one more line
   than `wc -l`.*
2. **v75 recurrence, in the pass that inherited v75.** The scope proposal published *"the true count is
   7 artifacts / 8 paths"*. The matcher actually returns **15 hits / 10 artifacts**; 7/8 reproduces only
   after silently dropping `research/standards_analysis.md`, `NOVA_REVIEW_SUMMARY.md` and
   `SUBMISSION_PROCESS.md`. **The filter was the blind spot, and it was unpublished** — the exact shape
   of the carry this pass was handed.
3. **v63 recurrence, inverted.** The proposal framed this as *"C418's denominator is 4, the truth is
   7"*. But the 7 **omits two of C418's four**: `registries/README.md:6` is prose under `## Overview`
   (no IANA heading) and `draft-web4-core-00.xml:319` is `<section title="IANA Considerations">`
   (invisible to a markdown-heading regex). A set that omits half the audited set is **a different
   measurement, not a correction**. Withdrawn entirely — see §G.
4. **v73 recurrence**: the over-narrow `See LCT spec` matcher hid 4 of 5 citing sites, including the
   only mitigating one. §D.
5. **v44 fired on my own draft headline.** The first N-candidate was *"the standard's index never links
   the canonical LCT spec"* — stated verbatim at **`C114:56`** by this lineage's own 2nd delta, and
   path-corrected at `C146:46`. It is C75 3.1, HELD eight passes. Killed before drafting.
6. **The forbidden instrument, in the row that names it.** §B.1 — the first routing probe used spec
   files, which C382 §I.3 explicitly forbade in the same sentence that set the task.
7. **A broken comment-stripper produced a false negative about an artifact.** My first pass stripped
   `//` inside string literals, so `"https://web4.io/contexts/lct.jsonld"` was mangled and the canonical
   §2.3 block was reported as unparseable. It parses. *An unqualified negative is a claim about your
   instrument* (v73).

---

## §G — WITHDRAWN, and routed to C458 instead of charged

**The "C418 denominator error" is withdrawn as a finding of this pass.** C418-N1's predicate is
**self-declaration** — `C418:201-203`: *"`registries/README.md:6` opens: 'These registries define the
IANA considerations for Web4 protocol parameters.' That is the cluster's own statement of what it is.
**Three other artifacts make the same claim for themselves**."* Its four are whole-standard IANA
statements (the registry cluster + three IETF submission vehicles). The extras a heading sweep finds are
**per-document** IANA sections — `web4-lct.md` §9, `web4-witness.md` §9, `extensibility_framework.md` §3
each register what *that document* defines, which is ordinary IETF practice, not disagreement. **C418's
verdict is untouched.**

What *is* real is an **instrument gap, not a verdict error**: C418 published its four-member set with
**no matcher and no exclusion rule** (checked — no IANA sweep appears among its commands), so the
self-declaration predicate is defensible by intent and unpublished as a predicate. **ROUTED to C458**
(next registries slot) as a v40 instrument row. Per v71, a falsifier for another lineage's routed row is
**a correction to route, not a row to take**.

For C458's use, the corrected measurement, published as a **set** with its filter, not a number:

```
matcher:  git grep -niE '^ *(#{1,6}|[0-9]+\.) *.*IANA Considerations' -- web4-standard/
returns:  15 hits / 10 artifacts
BLIND TO: prose declarations (registries/README.md:6) and XML (<section title=…>, draft-web4-core-00.xml:319)
          — i.e. it cannot see 2 of C418's own 4.
```

Union across both predicates: **9 paths / 8 authoring decisions** carry a Web4 IANA-Considerations
statement (`WEB4_WITNESSING_SPECIFICATION.md` ≡ `protocols/web4-witnessing.md`, blob `7984a8e5`).

---

## §H — Carry to C462 (next `web4-lct` delta = C422 + 40)

1. **C382-N1 + N2 are UNDELIVERED after two passes** — re-routed. Do **not** re-derive them; do not
   re-file them as new. §B.1 measured the receiver side: nothing received them, and per §E **nothing
   could have**. If a receipt surface exists by C462, probe it; if not, stop asking successors to.
2. **The corrected event trigger (C382 §I.1) is VALIDATED** — first availability, correctly silent.
   Keep it: movers of `web4-core/src/{lct,attestation,ratchet}.rs`, baselines `2ec6ae09`/`0e997079`/
   `7b048a78`. Coverage gap unchanged: `lib.rs`, `python/`.
3. **§B.5 stays closed.** Gate `0/1/0/0/1/0`, third consecutive identical reading. Re-run the one
   matcher; re-derive only a tree whose count changed.
4. **DO NOT re-run §B.2** (schema execution of §1) — it is done, it yields ZERO net-new, and the
   15-error → C74-row mapping is published above.
5. **C422-N1 is in the C70 bundle**, not a standalone row. **C422-I1 is a widening of C75 3.1**, not a
   finding. Neither is a novel row for C462 to count.
6. **The §9 block is now fully enumerated**: item 1 refuted (C74), items 2+4 = B19, item 3 = C422-N1.
   §9 is exhausted — do not re-walk it.
7. **Baselines for C462**: target `5f68a5c7`, canonical `d89595e8`, **`web4-standard/README.md`
   `10aa3e31`** (moved this window), `lct.rs` `2ec6ae09`, `attestation.rs` `0e997079`, `ratchet.rs`
   `7b048a78`, `lct.schema.json` `9bcfe598`, `WEB4_WITNESSING_SPECIFICATION.md` ≡
   `protocols/web4-witnessing.md` `7984a8e5`.
8. **Cadence: datapoint 17.** Target inert 184 days. C382 predicted the yield would be in the periphery
   and recommended budgeting accordingly. It was — but the periphery yielded **one LOW-MED and one
   INFO**, both bundle-joins rather than standalone rows, after policy review killed both drafted
   headlines. Two-pass pattern is now **three-pass and decaying**: C342 and C382 each yielded 2 MED from
   the periphery; C422 yielded 0 MED. **Recommend the operator consider retiring or lengthening this
   lineage's rotation** — 10 passes, 184 days frozen, and D0 (the flagship that gates every remediation)
   is unanswered since 2026-06-19. That is an operator call, not self-applied.

---

## §I — Policy review record

Reviewer: subagent (v2 protocol Step 4, PolicyEntity placeholder). Decision: **REVISE**, 7 binding
changes (R1–R7), all applied above. Scope, file budget, phase and zero-mutation constraints approved as
proposed; **the findings were not**.

- **N1 REFUTED as framed** → withdrawn to §G (route), surviving core re-filed as §C at LOW-MED.
- **N2 REFUTED as net-new** → re-filed as §D, INFO, widening of C75 3.1 per `C186:19`.
- Premises 1 (gate matcher) and 5 (B1 denominator) **reproduced exactly** by the reviewer and published
  verbatim per R6.
- **23rd consecutive falsified headline** in this lineage.
- **Reviewer's own corrections verified (v52), and one did not reproduce**: it reported the heading sweep
  as *"16 hits / 10 artifacts"*; the sweep returns **15 hits / 10 artifacts**. **Its items are right and
  its count is off by one** — which is v63 read in the other direction, and the reason the *set* is
  published above rather than the number. Its other six corrections (§9 four items / B19 anchor exact;
  `t3-v3-tensors.md:16`; C418's predicate sentence; `C75:54`; `WEB4_WITNESSING_SPECIFICATION.md:17`;
  `registries/README.md` witness count 0) all reproduce exactly.

---

## Review-gate block (accountability self-audit)

```
surface: C422 audit document   act: publish audit findings + route to operator/C458 (no state mutation)
S: low/reversible [construct: doc-only, ZERO mutation, single new file under docs/audits/; git-revertible]
R: n/a [construct: no caller-reachable surface created]
W: pass [construct: signed commit on worker/web4-20260820-120000, PR reviewed by a separate track]
O: pass [construct: policy review (Step 4) precedes every finding; both drafted headlines killed BEFORE §C/§D were written]
A: pass [construct: this document commits findings, their evidence-basis, the withdrawn claim (§G) and 7 own-errors (§F) together]
V: present [construct: §C.5 self-refutation; §G full withdrawal + route; §H.8 escalates lineage retirement to the operator rather than self-applying]
verdict: PASS
```
